# Tuya Power Meter - Configuration Example
# Copy this file to config.yaml and fill in your values

device:
  ip: 192.168.1.100          # Device IP on your LAN, or "auto" to discover (see below)
  id: your_device_id_here    # Tuya device ID (from IoT Platform)
  local_key: your_local_key  # 16-char local key (from IoT Platform)
  version: "3.5"             # Protocol version: "3.3", "3.4", or "3.5"
  # mac: "aa:bb:cc:dd:ee:ff" # Optional, used by ip: auto for ARP-based lookup
  # device_type: at4pw       # Optional: "at4pw" or "ct" (auto-detected if omitted)

poll_secs: 10                # How often to poll the meter (seconds)

# bcast_addr: 192.168.1.255  # Optional: subnet-directed broadcast for ip: auto.
                             # Defaults to a /24 derived from your local IP.
                             # Set explicitly if your LAN isn't /24 (e.g. 10.0.0.255 for /16).

# Optional: MQTT bridge with Home Assistant auto-discovery
# Remove this section if you only need CLI / log-only mode
mqtt:
  host: "192.168.1.1"        # MQTT broker address (IPv6 without brackets)
  port: 1883                 # MQTT broker port
  node_id: at4pw             # HA discovery node ID & topic prefix

# Optional: JSONL logging with rotation
# Remove this section if you don't need local logging
log:
  file: /var/log/tuya-meter.jsonl  # Log file path
  max_lines: 10000                 # Rotate after N lines (optional)
  rotate_interval: "24h"           # Rotate every interval: "5h", "1d", "30m" (optional)
